# Mesh Network Mapper visualizer (ground tool)
# Plots detected drone positions and ESP-NOW mesh node positions on a map.
# Reads from session recordings or live WebSocket feed.
# Usage: python -m analyzerex.tools.mesh_mapper [--file session.json]
