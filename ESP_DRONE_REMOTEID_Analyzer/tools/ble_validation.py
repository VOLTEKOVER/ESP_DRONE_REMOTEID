# BLE Validation Script (ground tool)
# macOS/Windows BLE receiver test — scans for RID BLE advertisements.
# Uses bleak (cross-platform BLE library).
# Usage: python -m analyzerex.tools.ble_validation [--duration 60]
