# WiFi + BLE RID Receiver (ground scanner)
# Scans for nearby drone RID transmissions and decodes them.
# Requires monitor-mode WiFi adapter + BLE adapter.
# Usage: python -m analyzerex.tools.scanner_wifi_ble [--wifi-interface wlan0] [--ble]
