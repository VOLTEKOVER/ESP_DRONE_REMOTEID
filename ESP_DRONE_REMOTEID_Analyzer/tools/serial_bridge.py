# macOS Serial Bridge (dev tool)
# socat wrapper for Docker-based ESP-IDF development on macOS.
# Bridges USB serial to TCP for container access.
# Usage: python -m analyzerex.tools.serial_bridge [--port /dev/ttyUSB0] [--tcp-port 1234]
