# ESP DRONE REMOTEID Analyzer + Ground Tools
# Install: pip install -r requirements.txt

# Core
scapy>=2.5.0

# WebSocket bridge
simple-websocket-server>=0.4.0

# Desktop GUI (optional — omit for headless/server only)
pywebview>=4.4.1

# Tools extras (optional — omit if not using tools)
bleak>=0.22.0           # BLE validation (macOS/Windows)
pyserial>=3.5           # Meshtastic bridge, NVS provisioning
pyyaml>=6.0             # OpenAPI spec parsing
cryptography>=41.0.0    # ECDSA signature verification

# Web UI serves bundled HTML, but no Python web framework needed
# (WebSocket handles real-time data)
